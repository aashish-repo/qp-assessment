package com.qpassessment.constants;


/**
 * We can maintain the item inventory level by current stock of item
 * created one field in db to track the level and admin can update the stock for item as well
 */

public enum InventoryLevel {

    FULL, EMPTY, HALF

}
